worker_processes 24;

events {
    worker_connections 1024;
}

http {
    access_log off;

    server {
        listen 80;
        server_name localhost;

        location / {
            root /usr/share/nginx/html;
            index index.html;
            try_files $uri $uri/ /index.html;
        }
    }
}
